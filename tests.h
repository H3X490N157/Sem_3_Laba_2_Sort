#include <iostream>
#include <string>

#include "ArraySequence.h"
#include "ShellSorter.h"
#include "HeapSorter.h"
#include "QuickSorter.h"

/*void PrintSequence(ArraySequence<int>& seq, const std::string& title) {
    std::cout << "\n" << title << ":\n";
    for (int i = 0; i < seq.GetLength(); ++i) {
        std::cout << seq.Get(i) << " ";
    }
    std::cout << "\n";
}*/